// write a program to find the length of a array.

#include<stdio.h>
void main (){
   int i,n,b=1;
   
   printf("Enter array size :- ");
  scanf("%d",&n);
   
 
     int count [n];
      for (i=0;i<=n;i++){
      	printf("n[%d]=",i);
      	scanf("%d",&count[n]);
      	b++;
	  }
	  printf("length of array is %d",b-1);
      
}
